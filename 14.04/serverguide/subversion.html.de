<!DOCTYPE html>
<html lang=de>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<title>Subversion</title>
<link rel="stylesheet" type="text/css" href="de.css">
<script type="text/javascript" src="jquery.js"></script><script type="text/javascript" src="jquery.syntax.js"></script><script type="text/javascript" src="yelp.js"></script>
</head>
<body id="home">
<script src="https://ssl.google-analytics.com/urchin.js" type="text/javascript"></script><script type="text/javascript">
        _uacct = "UA-1018242-8";
        urchinTracker();
      </script><div id="container">
<div id="container-inner">
<div id="mothership"><ul>
<li><a href="http://www.ubuntu.com/partners">Partners</a></li>
<li><a href="http://www.ubuntu.com/support">Support</a></li>
<li><a href="http://www.ubuntu.com/community">Community</a></li>
<li><a href="http://www.ubuntu.com">Ubuntu.com</a></li>
</ul></div>
<div id="header">
<h1 id="ubuntu-header"><a href="https://help.ubuntu.com/">Ubuntu Documentation</a></h1>
<ul id="main-menu">
<li><a class="main-menu-item current" href="../../">Official Documentation</a></li>
<li><a href="https://help.ubuntu.com/community/CommunityHelpWiki">Community Help Wiki</a></li>
<li><a href="http://community.ubuntu.com/contribute/documentation/">Contribute</a></li>
</ul>
</div>
<div id="menu-search"><div id="search-box">
<noscript><form action="https://www.google.com/cse" id="cse-search-box"><div>
<input type="hidden" name="cx" value="003883529982892832976:e2vwumte3fq"><input type="hidden" name="ie" value="UTF-8"><input type="text" name="q" size="21"><input type="submit" name="sa" value="Search">
</div></form></noscript>
<script>
                document.write('<form action="../../search.html" id="cse-search-box">');
                document.write('  <div>');
                document.write('    <input type="hidden" name="cof" value="FORID:9">');
                document.write('    <input type="hidden" name="cx" value="003883529982892832976:e2vwumte3fq">');
                document.write('    <input type="hidden" name="ie" value="UTF-8">');
                document.write('    <input type="text" name="q" size="21">');
                document.write('    <input type="submit" name="sa" value="Search">');
                document.write('  </div>');
                document.write('</form>');
              </script>
</div></div>
<div class="trails"><div class="trail">
<a href="../../14.04" class="trail">Ubuntu 14.04</a> » <a class="trail" href="index.html" title="Ubuntu–Server-Leitfaden">Ubuntu–Server-Leitfaden</a> » <a class="trail" href="version-control-system.html" title="Versionskontrollsystem">Versionskontrollsystem</a> » </div></div>
<div id="cwt-content" class="clearfix content-area"><div id="page">
<div id="content">
<div class="links nextlinks">
<a class="nextlinks-prev" href="git.html" title="Git">Zurück</a><a class="nextlinks-next" href="version-control-ref.html" title="Referenzen">Weiter</a>
</div>
<div class="hgroup"><h1 class="title">Subversion</h1></div>
<div class="region">
<div class="contents"><p class="para">Subversion ist ein Open Source–Versionskontrollsystem. Mit Subversion können Sie die Versionsgeschichte von Quelldateien und Dokumenten aufzeichnen. Es verwaltet Dateien und Ordner über die Zeit. In einem zentralen Repository ist ein Dateibaum angeordnet. Das Repository ist einem normalen Datenserver sehr ähnlich, mit der Ausnahme, dass es jede Änderung speichert, die jemals an Dateien oder Ordnern durchgeführt wurde.</p></div>
<div class="links sectionlinks" role="navigation"><ul>
<li class="links"><a class="xref" href="subversion.html#subversion-installation" title="Installation">Installation</a></li>
<li class="links"><a class="xref" href="subversion.html#subversion-configuration" title="Serverkonfiguration">Serverkonfiguration</a></li>
<li class="links"><a class="xref" href="subversion.html#access-methods" title="Zugriffsmethoden">Zugriffsmethoden</a></li>
</ul></div>
<div class="sect2 sect" id="subversion-installation"><div class="inner">
<div class="hgroup"><h2 class="title">Installation</h2></div>
<div class="region"><div class="contents">
<p class="para">Um auf das Subversion-Repository über das HTTP-Protokoll zugreifen zu können, müssen Sie einen Webserver installieren und konfigurieren. Ein bewährter Webserver hierfür ist Apache2. Schlagen Sie bitte im HTTP-Unterpunkt des Abschnitts über Apache2 nach, um mehr über die Installation</p>
<p class="para">Um Subversion zu installieren, geben Sie bitte folgendes in ein Terminal ein:</p>
<p class="para">
<div class="screen"><pre class="contents "><span class="cmd command">sudo apt-get install subversion apache2 libapache2-svn</span>
</pre></div>
	    </p>
</div></div>
</div></div>
<div class="sect2 sect" id="subversion-configuration"><div class="inner">
<div class="hgroup"><h2 class="title">Serverkonfiguration</h2></div>
<div class="region">
<div class="contents"><p class="para">Dieser Schritt setzt voraus, dass Sie die oben genannten Pakete auf Ihrem System installiert haben. Dieser Abschnitt erläutert, wie ein Subversion-Repository erstellt wird und wie auf das Projekt zugegriffen wird.</p></div>
<div class="sect3 sect" id="create-svn-repos"><div class="inner">
<div class="hgroup"><h3 class="title">Subversion-Repository erstellen</h3></div>
<div class="region"><div class="contents">
<p class="para">Das Subversion-Repository kann erstellt werden, indem man den folgenden Befehl in ein Terminal eingibt:</p>
<p class="para">
<div class="screen"><pre class="contents "><span class="cmd command">svnadmin create /path/to/repos/project</span>
</pre></div>
        </p>
</div></div>
</div></div>
<div class="sect3 sect" id="import-svn-files"><div class="inner">
<div class="hgroup"><h3 class="title">Dateien importieren</h3></div>
<div class="region"><div class="contents"><p class="para">Wenn Sie das Repository erstellt haben, können Sie Dateien in das Repository <span class="em emphasis">importieren</span>. Um einen Ordner zu importieren, geben Sie folgenden Befehl in ein Terminal ein: <div class="screen"><pre class="contents "><span class="cmd command">svn import /path/to/import/directory file:///path/to/repos/project</span>
</pre></div></p></div></div>
</div></div>
</div>
</div></div>
<div class="sect2 sect" id="access-methods"><div class="inner">
<div class="hgroup"><h2 class="title">Zugriffsmethoden</h2></div>
<div class="region">
<div class="contents">
<p class="para">Auf Subversion-Repository kann mittels vieler verschiedener Methoden zugegriffen werden - auf einer lokalen Festplatte oder über verschiedene Netzwerkprotokolle. Der Speicherort einer Paketquelle ist immer eine URL. Die Tabelle zeigt wie die verschiedenen URL-Schemata auf die verfügbaren Zugriffsmethoden abbilden.</p>
<div class="table">
<div class="title"><h3><span class="title">Zugriffsmethoden</span></h3></div>
<table summary="Zugriffsmethoden" style="border: solid 1px;">
<thead><tr>
<th class="td-colsep"><p class="para">Schema</p></th>
<th><p class="para">Zugriffsmethode</p></th>
</tr></thead>
<tbody>
<tr>
<td class="td-colsep"><p class="para">file://</p></td>
<td><p class="para">Direkter Zugriff auf das Repository (auf lokaler Festplatte)</p></td>
</tr>
<tr class="shade">
<td class="td-colsep"><p class="para">http://</p></td>
<td><p class="para">Zugriff auf einen Subversion-fähigen Apache2 Webserver über das WebDAV-Protokoll</p></td>
</tr>
<tr>
<td class="td-colsep"><p class="para">https://</p></td>
<td><p class="para">Das gleiche wie http://, nur mit SSL-Verschlüsselung</p></td>
</tr>
<tr class="shade">
<td class="td-colsep"><p class="para">svn://</p></td>
<td><p class="para">Zugriff auf einen svnserve-Server über ein spezielles Protokoll</p></td>
</tr>
<tr>
<td class="td-colsep"><p class="para">svn+ssh://</p></td>
<td><p class="para">Das gleiche wie svn://, aber durch einen SSH-Tunnel</p></td>
</tr>
</tbody>
</table>
</div>
<p class="para">In diesem Abschnitt wird die Konfiguration von Subversion für alle aufgeführten Zugriffsmethoden beleuchtet. Es werden die Grundfunktionen erläutert. Weitere Detail bezüglich erweiterter Funktionen finden Sie unter <a href="http://svnbook.red-bean.com/" class="ulink" title="http://svnbook.red-bean.com/">svn book</a>.</p>
</div>
<div class="sect3 sect" id="direct-repos-access"><div class="inner">
<div class="hgroup"><h3 class="title">Direkter Zugriff auf das Repository (file://)</h3></div>
<div class="region"><div class="contents">
<p class="para">Dies ist die einfachste aller Zugriffsmethoden. Sie benötigt keinen laufenden Subversion-Server. Diese Zugriffsmethode wird benutzt, um von der gleichen Maschine aus auf Subversion zuzugreifen. Der Syntax des entsprechenden Terminal-Befehls lautet:</p>
<p class="para">
<div class="screen"><pre class="contents "><span class="cmd command">svn co file:///path/to/repos/project</span>
</pre></div>
        </p>
<p class="para">oder</p>
<p class="para">
<div class="screen"><pre class="contents "><span class="cmd command">svn co file://localhost/path/to/repos/project</span>
</pre></div>
        </p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
		<p class="para">Wenn Sie keinen Hostnamen angeben, sind drei Schrägstriche (///) nötig - zwei für das Protokoll (in diesem Fall Datei) und einen Führungsstrich im Pfad. Wenn Sie einen Hostnamen angeben, müssen Sie zwei Schrägstriche verwenden.</p>
	</div></div></div></div>
<p class="para">Zugriffsberechtigungen auf das Repository hängen von den Berechtigungen im Dateisystem ab. Hat der Benutzer Lese- und Schreibrechte, kann er aus der Paketquelle heraus gehen und auch auf sie zugreifen.</p>
</div></div>
</div></div>
<div class="sect3 sect" id="access-via-webdav"><div class="inner">
<div class="hgroup"><h3 class="title">Zugriff über das WebDAV Protokoll (http://)</h3></div>
<div class="region"><div class="contents">
<p class="para">
                          To access the Subversion repository via WebDAV protocol, you must configure your Apache 2 web server. Add the following snippet between the 
                          <span class="em emphasis">&lt;VirtualHost&gt;</span> and <span class="em emphasis">&lt;/VirtualHost&gt;</span> elements in
                          <span class="file filename">/etc/apache2/sites-available/000-default.conf</span>, or another VirtualHost file:
                          </p>
<div class="code"><pre class="contents "> &lt;Location /svn&gt;
  DAV svn
  SVNParentPath /path/to/repos
  AuthType Basic
  AuthName "Your repository name"
  AuthUserFile /etc/subversion/passwd
  Require valid-user
 &lt;/Location&gt; 
</pre></div>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
		<p class="para">The above configuration snippet assumes that Subversion
    repositories are created under <span class="file filename">/path/to/repos</span>
    directory using <span class="cmd command">svnadmin</span> command and that the HTTP user has sufficent access rights to the files (see below).  They can be
    accessible using <span class="cmd command">http://hostname/svn/repos_name</span>
    url.</p>
    </div></div></div></div>
<p class="para">
         Changing the apache configuration like the above requires to reload
         the service with the following command
         </p>
<div class="screen"><pre class="contents ">    <span class="cmd command">sudo service apache2 reload</span>
</pre></div>
<p class="para">
         To import or commit files to your Subversion repository over
         HTTP, the repository should be owned by the HTTP user. In
         Ubuntu systems, the HTTP user is
         <span class="cmd command">www-data</span>.  To change the ownership of the
         repository files enter the following command from terminal
         prompt:
         </p>
<div class="screen"><pre class="contents ">    <span class="cmd command">sudo chown -R www-data:www-data /path/to/repos</span>
</pre></div>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
        <p class="para">Wenn Sie den Besitz des Repositories auf <span class="cmd command">www-data</span> ändern, werden Sie mit keinem Benutzer außer <span class="cmd command">www-data</span> in der Lage sein, Dateien über Ausführung des Befehls <span class="cmd command">svn import file:///</span> in das Repository zu importieren oder einzuspielen.</p>
        </div></div></div></div>
<p class="para">Anschließend müssen Sie die Datei <span class="file filename">/etc/subversion/passwd</span> erstellen, welches die Benutzer-Anmeldeinformationen enthält. Um die Datei zu erstellen, geben Sie folgenden Befehl (welcher die Datei erstellt und den ersten Benutzer hinzufügt) in eine Befehlszeile ein:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo htpasswd -c /etc/subversion/passwd user_name</span>
</pre></div>
<p class="para">Um zusätzliche Benutzer hinzuzufügen, lassen Sie die Option <span class="em emphasis">"-c"</span> weg, da diese Option die alte Datei ersetzt. Benutzen Sie stattdessen folgende Form:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo htpasswd /etc/subversion/passwd user_name</span>
</pre></div>
<p class="para">Dieser Befehl wird Sie dazu auffordern, Ihr Passwort einzugeben. Wenn Sie dies getan haben, wird der Benutzer hinzugefügt. Um nun auf das Repository zuzugreifen, können Sie den folgenden Befehl ausführen:</p>
<div class="screen"><pre class="contents "><span class="cmd command">svn co http://servername/svn</span></pre></div>
<div class="note note-warning" title="Warnung"><div class="inner"><div class="region"><div class="contents">
		<p class="para">Das Passwort wird als einfacher Text übermittelt. Falls Sie befürchten, dass andere das Passwort ausspähen, sollten Sie SSL-Verschlüsselung benutzen. Weitere Details hierüber erfahren Sie im nächsten Abschnitt.</p>
	</div></div></div></div>
</div></div>
</div></div>
<div class="sect3 sect" id="access-via-webdav-with-ssl"><div class="inner">
<div class="hgroup"><h3 class="title">Zugriff über das SSL-verschlüsselte WebDAV Protokoll (https://)</h3></div>
<div class="region"><div class="contents">
<p class="para"> 
					Accessing Subversion repository via WebDAV protocol with SSL 
					encryption (https://) is similar to http:// except that you 
					must install and configure the digital certificate in your 
					Apache2 web server.  To use SSL with Subversion add the above
                                        Apache2 configuration to <span class="file filename">/etc/apache2/sites-available/default-ssl.conf</span>.
                                        For more information on setting up Apache2 with SSL see <a class="xref" href="httpd.html#https-configuration" title="HTTPS-Konfiguration">HTTPS-Konfiguration</a>.
					</p>
<p class="para">Sie können ein digitales Zertifikat installieren, indem es von einer signierenden Autorität ausgegeben wird. Alternativ können Sie Ihr eigenes, selbst-signiertes Zertifikat installieren.</p>
<p class="para">Dieser Abschnitt setzt voraus, dass Sie auf ihrem Apache 2 Webserver ein digitales Zertifikat installiert und konfiguriert haben. Um nun auf das Subversion-Repository zuzugreifen, lesen Sie bitte den vorangegangen Abschnitt. Die Zugangsmethoden sind, bis auf das Protokoll, exakt die gleichen. Sie müssen https:// benutzen, um auf das Repository Zugriff zu haben.</p>
</div></div>
</div></div>
<div class="sect3 sect" id="access-via-custom-protocol"><div class="inner">
<div class="hgroup"><h3 class="title">Zugriff über spezielles Protokoll (svn://)</h3></div>
<div class="region"><div class="contents">
<p class="para">Wenn das Subversion-Repository erstellt wurde, können Sie die Zugangssteuerung konfigurieren. Sie können die <span class="file filename"> /path/to/repos/project/conf/svnserve.conf</span>-Datei editieren, um die Zugangssteuerung zu konfigurieren. Um zum Beispiel die Authentifizierung zu erstellen, können Sie folgende Zeilen in der Konfigurationsdatei auskommentieren:</p>
<div class="code"><pre class="contents "># [general]
# password-db = passwd</pre></div>
<p class="para">Nachdem Sie obige Zeilen auskommentiert haben, können Sie die Benutzerliste in der Datei »passwd« beibehalten. Ändern Sie die Datei <span class="file filename">passwd </span> im selben Verzeichnis und fügen Sie den neuen Benutzer hinzu. Der Syntax lautet wie folgt:</p>
<div class="code"><pre class="contents ">Benutzername = Passwort</pre></div>
<p class="para">Weitere Details finden Sie in der Datei</p>
<p class="para">Um nun auf Subversion über das spezielle svn://-Protokoll entweder von der selben oder einer anderen Maschine aus zuzugreifen, können Sie svnserver mit dem Befehl svnserve ausführen. Der Syntax lautet wie folgt:</p>
<div class="code"><pre class="contents ">$ svnserve -d --foreground -r /path/to/repos
# -d -- Dienst-Modus
# --foreground -- im Vordergrund ausführen (nützlich für debugging)
# -r -- Wurzel des bedienten Verzeichnisses

Weitere Details erhalten Sie mit:
$ svnserve --help</pre></div>
<p class="para">Wenn Sie diesen Befehl ausführen, fängt Subversion an, auf dem Standard-Port (3690) zu lauschen. Um auf das Projekt-Repository zuzugreifen, müssen Sie folgenden Befehl in einem Terminal ausführen:</p>
<p class="para">
<div class="screen"><pre class="contents "><span class="cmd command">svn co svn://hostname/project project --username user_name</span>
</pre></div>
                                        </p>
<p class="para">Abhängig von der Serverkonfiguration, wird ein Passwort abgefragt. Wenn Sie sich authentifiziert haben, wird der Code aus dem Subversion-Repository überprüft. Um das Projekt-Repository mit der lokalen Kopie zu synchronisieren, können Sie den <span class="cmd command">update</span>-Unterbefehl ausführen. Die Syntax des in ein Terminal einzugebenden Befehls lautet wie folgt:</p>
<p class="para">
<div class="screen"><pre class="contents "><span class="cmd command">cd project_dir ; svn update</span>
</pre></div>
                                        </p>
<p class="para">Um mehr über die Benutzung jeden einzelnen Subversion Unterbefehls zu erfahren, sollten Sie sich das Handbuch durchlesen. Um beispielsweise mehr über den co (checkout)-Befehl zu erfahren, führen Sie bitte folgenden Befehl in einem Terminal aus:</p>
<p class="para"><div class="screen"><pre class="contents "><span class="cmd command">svn co help</span></pre></div></p>
</div></div>
</div></div>
<div class="sect3 sect" id="access-via-custom-protocol-with-ssh"><div class="inner">
<div class="hgroup"><h3 class="title">Access via custom protocol with SSH encryption (svn+ssh://)</h3></div>
<div class="region"><div class="contents">
<p class="para">Der Konfigurationsprozess ist derselbe wie in der svn://-Methode. Weitere Details finden Sie im vorangegangen Abschnitt nach. Der folgende Schritt setzt voraus, dass Sie den vorangegangenen Schritt befolgt haben und den Subversion-Server mit dem <span class="app application">svnserve</span>-Befehl gestartet haben.</p>
<p class="para">Es wird ebenfalls vorausgesetzt, dass der SSH-Server auf dieser Maschine läuft und dass er eingehende Verbindungen erlaubt. Um dies zu bestätigen, versuchen Sie bitte, sich mittels SSH auf dieser Maschine einzuloggen. Wenn Sie sich einloggen können, funktioniert alles wie gewünscht. Wenn Sie sich nicht einloggen können, beheben Sie dies, bevor Sie fortfahren.</p>
<p class="para">Das svn+ssh://-Protokoll wird benutzt, um auf das Subversion-Repository mittels SSL-Verschlüsselung zuzugreifen. Die Datenübertragung ist mittels dieser Methode verschlüsselt. Um auf das Projekt zugreifen zu können (beispielsweise mit einem Checkout), müssen Sie folgenden Syntax benutzen:</p>
<p class="para">
<div class="screen"><pre class="contents ">    <span class="cmd command">svn co svn+ssh://ssh_username@hostname/path/to/repos/project</span>
</pre></div>
                                        </p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents"><p class="para">Sie müssen den vollständigen Pfad (/Pfad/zu/Repository/Projekt) benutzen, um auf das Subversion-Repository mit dieser Zugriffsmethode zuzugreifen.</p></div></div></div></div>
<p class="para">Abhängig von der Serverkonfiguration wird ein Passwort abgefragt. Sie müssen das Passwort eingeben, um den Login über SSH zu verwenden. Wenn Sie authentifiziert sind, wird der Code im Subversion-Repository überprüft.</p>
</div></div>
</div></div>
</div>
</div></div>
</div>
<div class="links nextlinks">
<a class="nextlinks-prev" href="git.html" title="Git">Zurück</a><a class="nextlinks-next" href="version-control-ref.html" title="Referenzen">Weiter</a>
</div>
<div class="clear"></div>
</div>
<div id="pagebottom"></div>
</div></div>
</div>
<div id="footer"><p>The material in this document is available under a free license, see <a href="../../legal.html">Legal</a> for details.<br>
          For information on contributing see the <a href="https://wiki.ubuntu.com/DocumentationTeam">Ubuntu Documentation Team wiki page</a>.
          To report errors in this serverguide documentation, <a href="https://bugs.launchpad.net/serverguide">file a bug report</a>.</p></div>
</div>
</body>
</html>
