<!DOCTYPE html>
<html lang=de>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<title>Kerberos und LDAP</title>
<link rel="stylesheet" type="text/css" href="de.css">
<script type="text/javascript" src="jquery.js"></script><script type="text/javascript" src="jquery.syntax.js"></script><script type="text/javascript" src="yelp.js"></script>
</head>
<body id="home">
<script src="https://ssl.google-analytics.com/urchin.js" type="text/javascript"></script><script type="text/javascript">
        _uacct = "UA-1018242-8";
        urchinTracker();
      </script><div id="container">
<div id="container-inner">
<div id="mothership"><ul>
<li><a href="https://partners.ubuntu.com">Partners</a></li>
<li><a href="https://www.ubuntu.com/support/community-support">Support</a></li>
<li><a href="https://community.ubuntu.com">Community</a></li>
<li><a href="https://www.ubuntu.com">Ubuntu.com</a></li>
</ul></div>
<div id="header">
<h1 id="ubuntu-header"><a href="https://help.ubuntu.com/">Ubuntu Documentation</a></h1>
<ul id="main-menu">
<li><a class="main-menu-item current" href="../../">Official Documentation</a></li>
<li><a href="https://help.ubuntu.com/community/CommunityHelpWiki">Community Help Wiki</a></li>
<li><a href="https://community.ubuntu.com/t/contribute/26">Contribute</a></li>
</ul>
</div>
<div id="menu-search"><div id="search-box">
<noscript><form action="https://www.google.com/cse" id="cse-search-box"><div>
<input type="hidden" name="cx" value="003883529982892832976:e2vwumte3fq"><input type="hidden" name="ie" value="UTF-8"><input type="text" name="q" size="21"><input type="submit" name="sa" value="Search">
</div></form></noscript>
<script>
                document.write('<form action="../../search.html" id="cse-search-box">');
                document.write('  <div>');
                document.write('    <input type="hidden" name="cof" value="FORID:9">');
                document.write('    <input type="hidden" name="cx" value="003883529982892832976:e2vwumte3fq">');
                document.write('    <input type="hidden" name="ie" value="UTF-8">');
                document.write('    <input type="text" name="q" size="21">');
                document.write('    <input type="submit" name="sa" value="Search">');
                document.write('  </div>');
                document.write('</form>');
              </script>
</div></div>
<div class="trails"><div class="trail">
<a href="../../16.04" class="trail">Ubuntu 16.04</a> » <a class="trail" href="index.html" title="Ubuntu–Server-Leitfaden">Ubuntu–Server-Leitfaden</a> » <a class="trail" href="network-authentication.html" title="Netzwerk-Legitimation">Netzwerk-Legitimation</a> » </div></div>
<div id="cwt-content" class="clearfix content-area"><div id="page">
<div id="content">
<div class="links nextlinks">
<a class="nextlinks-prev" href="kerberos.html" title="Kerberos">Zurück</a><a class="nextlinks-next" href="sssd-ad.html" title="SSSD and Active Directory">Weiter</a>
</div>
<div class="hgroup"><h1 class="title">Kerberos und LDAP</h1></div>
<div class="region">
<div class="contents">
<p class="para">
    Most people will not use Kerberos by itself; once an user is authenticated
    (Kerberos), we need to figure out what this user can do (authorization).
    And that would be the job of programs such as
    <span class="app application">LDAP</span>.
    </p>
<p class="para">Die Replikation einer Kerberos-Principal-Datenbank zwischen zwei Servern kann kompliziert werden und fügt eine zusätzliche Datenbank in Ihr Netzwerk hinzu. Glücklicherweise kann MIT-Kerberos so konfiguriert werden, dass ein <span class="app application">LDAP</span>-Verzeichnis als Principal-Datenbank verwendet wird. Dieser Abschnitt beschreibt die Konfiguration eines primären und sekundären Kerberos-Servers, bei der ein <span class="app application">OpenLDAP</span> als Principal-Datenbank verwendet wird.</p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
       <p class="para">
       The examples presented here assume 
       <span class="app application">MIT Kerberos</span> and 
       <span class="app application">OpenLDAP</span>.
       </p>
    </div></div></div></div>
</div>
<div class="links sectionlinks" role="navigation"><ul>
<li class="links"><a class="xref" href="kerberos-ldap.html#kerberos-ldap-openldap" title="OpenLDAP konfigurieren">OpenLDAP konfigurieren</a></li>
<li class="links"><a class="xref" href="kerberos-ldap.html#kerberos-ldap-primary-kdc" title="Konfiguration des primären KDC">Konfiguration des primären KDC</a></li>
<li class="links"><a class="xref" href="kerberos-ldap.html#kerberos-ldap-secondary-kdc" title="Sekundäre KDC-Konfiguration">Sekundäre KDC-Konfiguration</a></li>
<li class="links"><a class="xref" href="kerberos-ldap.html#kerberos-ldap-resources" title="Quellen">Quellen</a></li>
</ul></div>
<div class="sect2 sect" id="kerberos-ldap-openldap"><div class="inner">
<div class="hgroup"><h2 class="title">OpenLDAP konfigurieren</h2></div>
<div class="region"><div class="contents">
<p class="para">Das notwendige <span class="em emphasis">Schema</span> muss zunächst auf einen <span class="app application">OpenLDAP</span>-Server geladen werden, der über eine Netzwerkverbindung zu den primären und sekundären KDCs verfügt. Der Rest dieses Abschnitts geht davon aus, dass Sie außerdem eine LDAP-Replikation zwischen mindestens zwei Servern eingerichten haben. Informationen über das Aufsetzen von OpenLDAP finden Sie in <a class="xref" href="openldap-server.html" title="OpenLDAP-Server">OpenLDAP-Server</a>.</p>
<p class="para">Es ist außerdem notwendig, dass OpenLDAP für TLS- und SSL-Verbindungen konfiguriert ist, sodass der Datenaustausch zwischen KDC und LDAP-Server verschlüsselt wird. Details dazu finden sich in <a class="xref" href="openldap-server.html#openldap-tls" title="TLS">TLS</a>.</p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
         <p class="para">
         <span class="file filename">cn=admin,cn=config</span> is a user we created with 
rights to edit the ldap database. Many times it is the RootDN. Change its 
value to reflect your setup.
         </p>
      </div></div></div></div>
<div class="list itemizedlist"><ul class="list itemizedlist">
<li class="list itemizedlist">
          <p class="para">Um ein Schema in das LDAP zu laden, installieren Sie das Software-Paket <span class="app application">krb5-kdc-ldap</span> auf dem LDAP-Server. Geben Sie in einer Befehlszeile ein:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo apt install krb5-kdc-ldap</span>
</pre></div>

        </li>
<li class="list itemizedlist">
          <p class="para">Als nächstes entpacken Sie die <span class="file filename">kerberos.schema.gz</span>-Datei:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo gzip -d /usr/share/doc/krb5-kdc-ldap/kerberos.schema.gz</span>
<span class="cmd command">sudo cp /usr/share/doc/krb5-kdc-ldap/kerberos.schema /etc/ldap/schema/</span>
</pre></div>

        </li>
<li class="list itemizedlist">

           <p class="para">Das <span class="em emphasis">kerberos</span> Schema muss im Baum  <span class="em emphasis">cn=config</span> hinzugefügt werden. Das Vorgehen, um ein neues Schema zu <span class="app application">slapd</span> hinzuzufügen. ist auch detailliert in <a class="xref" href="openldap-server.html#openldap-configuration" title="Modifying the slapd Configuration Database">Modifying the slapd Configuration Database</a> beschrieben.</p>

           <div class="steps"><div class="inner"><ol class="steps">
<li class="steps">
                <p class="para">Zuerst erstellen Sie eine Konfigurationsdatei mit dem Namen <span class="file filename">schema_convert.conf</span> oder einem ähnlich lautenden Namen, welche die folgenden Zeilen beinhaltet:</p>

<div class="code"><pre class="contents ">include /etc/ldap/schema/core.schema
include /etc/ldap/schema/collective.schema
include /etc/ldap/schema/corba.schema
include /etc/ldap/schema/cosine.schema
include /etc/ldap/schema/duaconf.schema
include /etc/ldap/schema/dyngroup.schema
include /etc/ldap/schema/inetorgperson.schema
include /etc/ldap/schema/java.schema
include /etc/ldap/schema/misc.schema
include /etc/ldap/schema/nis.schema
include /etc/ldap/schema/openldap.schema
include /etc/ldap/schema/ppolicy.schema
include /etc/ldap/schema/kerberos.schema
</pre></div>


                </li>
<li class="steps">

                  <p class="para">Erstellen Sie einen temporären Ordner, um die LDIF-Dateien zu sichern:</p>
<div class="screen"><pre class="contents "><span class="cmd command">mkdir /tmp/ldif_output</span>
</pre></div>

                </li>
<li class="steps">

                  <p class="para">Benutzen Sie nun <span class="app application">slapcat</span> um die Schema-Dateien zu konvertieren:</p>

<div class="screen"><pre class="contents "><span class="cmd command">slapcat -f schema_convert.conf -F /tmp/ldif_output -n0 -s \
"cn={12}kerberos,cn=schema,cn=config" &gt; /tmp/cn=kerberos.ldif</span>
</pre></div>

                  <p class="para">Passen Sie obige Datei- und Pfadnamen auf ihre eigenen an, falls Sie sich unterscheiden sollten.</p>
                
                </li>
<li class="steps">

                  <p class="para">BearbeitenBearbeiten Sie die Datei <span class="file filename">/tmp/cn\=kerberos.ldif</span>, indem Sie folgende Attribute ändern:</p>

<div class="code"><pre class="contents ">dn: cn=kerberos,cn=schema,cn=config
...
cn: kerberos
</pre></div>

                  <p class="para">Dann entfernen Sie die folgenden Zeilen am Ende der Datei:</p>

<div class="code"><pre class="contents ">structuralObjectClass: olcSchemaConfig
entryUUID: 18ccd010-746b-102d-9fbe-3760cca765dc
creatorsName: cn=config
createTimestamp: 20090111203515Z
entryCSN: 20090111203515.326445Z#000000#000#000000
modifiersName: cn=config
modifyTimestamp: 20090111203515Z
</pre></div>

                    <p class="para">Die Attributwerte können variieren; stellen Sie einfach sicher, dass die Attribute entfernt werden.</p>

                </li>
<li class="steps">

                  <p class="para">Laden Sie das neue Schema mit <span class="app application">ldapadd</span>:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo ldapadd -Q -Y EXTERNAL -H ldapi:/// -f /tmp/cn\=kerberos.ldif</span>
</pre></div>

                </li>
<li class="steps">
        
                  <p class="para">Fügen Sie einen Index zum Attribut <span class="em emphasis">krb5principalname</span> hinzu:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo ldapmodify -Q -Y EXTERNAL -H ldapi:///</span>
<span class="output computeroutput">
<span class="input userinput">dn: olcDatabase={1}mdb,cn=config
add: olcDbIndex
olcDbIndex: krbPrincipalName eq,pres,sub</span>

modifying entry "olcDatabase={1}mdb,cn=config"</span>
</pre></div>
      
                </li>
<li class="steps">
        
                  <p class="para">Aktualisieren Sie abschließend die Access Control Lists (ACL):</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo ldapmodify -Q -Y EXTERNAL -H ldapi:///</span>
<span class="output computeroutput">
<span class="input userinput">dn: olcDatabase={1}mdb,cn=config
replace: olcAccess
olcAccess: to attrs=userPassword,shadowLastChange,krbPrincipalKey by
 dn="cn=admin,dc=example,dc=com" write by anonymous auth by self write by * none
-
add: olcAccess
olcAccess: to dn.base="" by * read
-
add: olcAccess
olcAccess: to * by dn="cn=admin,dc=example,dc=com" write by * read</span>

modifying entry "olcDatabase={1}mdb,cn=config"
</span>
</pre></div>
      
                </li>
</ol></div></div>    
        </li>
</ul></div>
<p class="para">Das ist alles. Ihr LDAP-Verzeichnis ist nun bereit, als Principal-Datenbank für Kerberos genutzt zu werden.</p>
</div></div>
</div></div>
<div class="sect2 sect" id="kerberos-ldap-primary-kdc"><div class="inner">
<div class="hgroup"><h2 class="title">Konfiguration des primären KDC</h2></div>
<div class="region"><div class="contents">
<p class="para">Nachdem <span class="app application">OpenLDAP</span> konfiguriert ist, wird es nun Zeit, den KDC zu konfigurieren,</p>
<div class="list itemizedlist"><ul class="list itemizedlist">
<li class="list itemizedlist">
          <p class="para">Installieren Sie zunächst die benötigten Software-Pakete, indem Sie auf der Befehlszeile folgendes eingeben:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo apt install krb5-kdc krb5-admin-server krb5-kdc-ldap</span>
</pre></div>

        </li>
<li class="list itemizedlist">

          <p class="para">Bearbeiten Sie nun <span class="file filename">/etc/krb5.conf</span> und fügen Sie die Optionen in den betreffenden Abschnitten hinzu.</p>

<div class="code"><pre class="contents ">[libdefaults]
        default_realm = EXAMPLE.COM

...

[realms]
        EXAMPLE.COM = {
                kdc = kdc01.example.com
                kdc = kdc02.example.com
                admin_server = kdc01.example.com
                admin_server = kdc02.example.com
                default_domain = example.com
                database_module = openldap_ldapconf
        }

...

[domain_realm]
        .example.com = EXAMPLE.COM


...

[dbdefaults]
        ldap_kerberos_container_dn = cn=krbContainer,dc=example,dc=com

[dbmodules]
        openldap_ldapconf = {
                db_library = kldap
                ldap_kdc_dn = "cn=admin,dc=example,dc=com"

                # this object needs to have read rights on
                # the realm container, principal container and realm sub-trees
                ldap_kadmind_dn = "cn=admin,dc=example,dc=com"

                # this object needs to have read and write rights on
                # the realm container, principal container and realm sub-trees
                ldap_service_password_file = /etc/krb5kdc/service.keyfile
                ldap_servers = ldaps://ldap01.example.com ldaps://ldap02.example.com
                ldap_conns_per_server = 5
        }
</pre></div>

          <div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
            <p class="para">Ändern Sie <span class="em emphasis">beispiel.de</span>, <span class="em emphasis">dc=beispiel,dc=de</span>, <span class="em emphasis">cn=admin,dc=beispiel,dc=de</span>, und <span class="em emphasis">ldap01.beispie.del</span> in die entsprechende Domäne, LDAP-Objekt und LDAP-Server in Ihrem Netzwerk.</p>
          </div></div></div></div>

        </li>
<li class="list itemizedlist">
        
          <p class="para">Verwenden Sie nun das Werkzeug <span class="app application">kdb5_ldap_util</span>, um den Realm zu erstellen:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo kdb5_ldap_util -D cn=admin,dc=beispiel,dc=de create -subtrees \ dc=beispiel,dc=de -r BEISPIEL.DE -s -H ldap://ldap01.bespiel.de</span>
</pre></div>
      
        </li>
<li class="list itemizedlist">
        
          <p class="para">Erzeugen Sie ein »Versteck« (engl. stash, ein Ort, an dem es verborgen wird) für das zum Verbinden mit dem LDAP-Server benutzte Passwort. Dieses  Passwort wird von den Optionen <span class="em emphasis">ldap_kdc_dn</span> und <span class="em emphasis">ldap_kadmin_dn</span> in <span class="file filename">/etc/krb5.conf</span> benutzt:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo kdb5_ldap_util -D cn=admin,dc=beispiel,dc=com stashsrvpw -f \ /etc/krb5kdc/service.keyfile cn=admin,dc=beispiel,dc=com</span>
</pre></div>
      
        </li>
<li class="list itemizedlist">
        
          <p class="para">Kopieren Sie das CA-Zertifikat vom LDAP Server</p>

<div class="screen"><pre class="contents "><span class="cmd command">scp ldap01:/etc/ssl/certs/cacert.pem .</span>
<span class="cmd command">sudo cp cacert.pem /etc/ssl/certs</span>
</pre></div>

          <p class="para">Und bearbeiten Sie die Datei <span class="file filename">/etc/ldap/ldap.conf</span> um das Zertifikat zu benutzen:</p>

<div class="code"><pre class="contents ">TLS_CACERT /etc/ssl/certs/cacert.pem
</pre></div>

          <div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
            <p class="para">Das Zertifikat muss auch zum sekundären KDC kopiert werden, um die Verbindung zu den LDAP-Servern mittels LDAPS zu ermöglichen.</p>
          </div></div></div></div>
      
        </li>
<li class="list itemizedlist">

          <p class="para"> 
          Start the Kerberos KDC and admin server:
          </p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo systemctl start krb5-kdc.service</span>
<span class="cmd command">sudo systemctl start krb5-admin-server.service</span>
</pre></div>
        </li>
</ul></div>
<p class="para">Sie können nun Kerberos-Principals zur LDAP-Datenbank hinzufügen. Diese werden zu allen anderen LDAP-Servern kopiert, die für Replikation konfiguriert wurden. Um einen Principal mittels des Werkzeugs <span class="app application">kadmin.local</span> hinzuzufügen, geben Sie folgendes ein:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo kadmin.local</span>
<span class="output computeroutput">Authenticating as principal root/admin@BEISPIEL.DE with password.
kadmin.local:  <span class="input userinput">addprinc -x dn="uid=steve,ou=people,dc=example,dc=com" steve</span>
WARNING: no policy specified for steve@BEISPIEL.DE; defaulting to no policy
Enter password for principal "steve@BEISPIEL.DE": 
Re-enter password for principal "steve@BEISPIEL.DE": 
Principal "steve@BEISPIEL.DE" created.</span>
</pre></div>
<p class="para">Es sollten nun die Attribute »krbPrincipalName«, »krbPrincipalKey«, »krbLastPwdChange« und »krbExtraData« dem Benutzerobjekt <span class="em emphasis">uid=steve,ou=people,dc=beispiel,dc=com</span> hinzugefügt worden sein. Benutzen Sie die Werkzeuge  <span class="app application">kinit</span> und <span class="app application">klist</span> um zu überprüfen, ob der Benutzer tatsächlich ein Ticket verteilt hat.</p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
        <p class="para">Wenn das Benutzerobjekt schon erstellt wurde, wird die Option <span class="em emphasis">-x dn="..."</span> benötigt, um die Kerberos-Attribute hinzuzufügen. Andernfalls wird ein neues <span class="em emphasis">Principal</span>-Objekt im Unterverzeichnisbaum des Realm erstellt.</p>
      </div></div></div></div>
</div></div>
</div></div>
<div class="sect2 sect" id="kerberos-ldap-secondary-kdc"><div class="inner">
<div class="hgroup"><h2 class="title">Sekundäre KDC-Konfiguration</h2></div>
<div class="region"><div class="contents">
<p class="para">Die Konfiguration eines sekundären KDC, das das LDAP-Backend nutzt, ist ähnlich der Konfiguration eines sekundären KDC, welches die normale Kerberos-Datenbank nutzt.</p>
<div class="steps"><div class="inner"><ol class="steps">
<li class="steps">
          <p class="para">Installieren Sie zunächst alle hierfür benötigten Pakete. Geben Sie folgendes in ein Terminal ein:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo apt install krb5-kdc krb5-admin-server krb5-kdc-ldap</span>
</pre></div>

        </li>
<li class="steps">

          <p class="para">Editieren Sie dann die Datei <span class="file filename">/etc/krb5.conf</span>, um das LDAP-Backend zu benutzen:</p>

<div class="code"><pre class="contents ">[libdefaults]
        default_realm = BEISPIEL.DE

...

[realms]
        BEISPIEL.DE = {
                kdc = kdc01.beispiel.de
                kdc = kdc02.beispiel.de
                admin_server = kdc01.beispiel.de
                admin_server = kdc02.beispiel.de
                default_domain = beispiel.de
                database_module = openldap_ldapconf
        }

...

[domain_realm]
        .beispiel.de = BEISPIEL.DE

...

[dbdefaults]
        ldap_kerberos_container_dn = dc=beispiel,dc=de

[dbmodules]
        openldap_ldapconf = {
                db_library = kldap
                ldap_kdc_dn = "cn=admin,dc=beispiel,dc=de"

                # Dieses Objekt benoetigt aktivierte Leserechte fuer
                # den Realm-Container, Principal-Container und Realm-Sub-Trees:
                ldap_kadmind_dn = "cn=admin,dc=beispiel,dc=de"

                # Dieses Objekt benoetigt aktivierte Lese- und Schreibrechte fuer
                # den Realm-Container, Principal-Container und Realm-Sub-Trees:
                ldap_service_password_file = /etc/krb5kdc/service.keyfile
                ldap_servers = ldaps://ldap01.beispiel.de ldaps://ldap02.beispiel.de
                ldap_conns_per_server = 5
        }
</pre></div>


        </li>
<li class="steps">
        
      <p class="para">Erstellen Sie das »Versteck« (engl. stash, ein Ort, an dem es verborgen wird) für das LDAP-Verbindungspasswort.</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo kdb5_ldap_util -D cn=admin,dc=beispiel,dc=com stashsrvpw -f \ /etc/krb5kdc/service.keyfile cn=admin,dc=beispiel,dc=com</span>
</pre></div>
      
        </li>
<li class="steps">

          <p class="para">Kopieren Sie nun das <span class="file filename">/etc/krb5kdc/.k5.BEISPIEL.DE</span><span class="em emphasis">Hauptschlüssel</span>-»Versteck« (Stash) vom <span class="em emphasis">primären KDC</span> auf den sekundären KDC. Stellen Sie dabei sicher, dass Sie eine verschlüsselte Verbindung, zum Beispiel mittels <span class="app application">scp</span>, benutzen oder benutzen Sie einen Wechseldatenträger.</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo scp /etc/krb5kdc/.k5.BEISPIEL.DE steve@kdc02.beispiel.de:~</span>
<span class="cmd command">sudo mv .k5.BEISPIEL.DE /etc/krb5kdc/</span>
</pre></div>

        <div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
          <p class="para">Nochmals: Ersetzen Sie »BEISPIEL.DE« mit der tatsächlichen Bezeichnung Ihres Realms.</p>
        </div></div></div></div>

        </li>
<li class="steps">

          <p class="para">
          Back on the <span class="em emphasis">Secondary KDC</span>, (re)start the ldap
	  server only,
          </p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo systemctl restart slapd.service</span>
</pre></div>

        </li>
<li class="steps">

          <p class="para">Starten Sie schließlich den <span class="app application">krb5-kdc</span>-Dienst:</p>

<div class="screen"><pre class="contents "><span class="cmd command">sudo systemctl start krb5-kdc.service</span>
</pre></div>

        </li>
<li class="steps">

          <p class="para">
          Verify the two ldap servers (and kerberos by extension) are in sync.
          </p>

        </li>
</ol></div></div>
<p class="para">Sie haben nun redundante KDCs in Ihrem Netzwerk. Mit redundanten LDAP-Servern sollten Sie in der Lage sein, Benutzer zu authentifizieren, selbst wenn ein LDAP-Server, ein Kerberos-Server oder ein LDAP-Server und ein Kerberos-Server ausfallen/ nicht erreichbar sind.</p>
</div></div>
</div></div>
<div class="sect2 sect" id="kerberos-ldap-resources"><div class="inner">
<div class="hgroup"><h2 class="title">Quellen</h2></div>
<div class="region"><div class="contents"><div class="list itemizedlist"><ul class="list itemizedlist">
<li class="list itemizedlist">
          <p class="para">Der <a href="http://web.mit.edu/Kerberos/krb5-1.6/krb5-1.6.3/doc/krb5-admin.html#Configuring-Kerberos-with-OpenLDAP-back_002dend" class="ulink" title="http://web.mit.edu/Kerberos/krb5-1.6/krb5-1.6.3/doc/krb5-admin.html#Configuring-Kerberos-with-OpenLDAP-back_002dend"> Kerberos Admin Guide</a> (englisch) stellt weitere Details zur Verfügung.</p>
        </li>
<li class="list itemizedlist">
          <p class="para">
          For more information on <span class="app application">kdb5_ldap_util</span> see 
          <a href="http://web.mit.edu/Kerberos/krb5-1.6/krb5-1.6.3/doc/krb5-admin.html#Global-Operations-on-the-Kerberos-LDAP-Database" class="ulink" title="http://web.mit.edu/Kerberos/krb5-1.6/krb5-1.6.3/doc/krb5-admin.html#Global-Operations-on-the-Kerberos-LDAP-Database">
          Section 5.6</a> and the 
          <a href="http://manpages.ubuntu.com/manpages/xenial/en/man8/kdb5_ldap_util.8.html" class="ulink" title="http://manpages.ubuntu.com/manpages/xenial/en/man8/kdb5_ldap_util.8.html">kdb5_ldap_util man page</a>.
          </p>
        </li>
<li class="list itemizedlist">
          <p class="para">
          Another useful link is the <a href="http://manpages.ubuntu.com/manpages/xenial/en/man5/krb5.conf.5.html" class="ulink" title="http://manpages.ubuntu.com/manpages/xenial/en/man5/krb5.conf.5.html">krb5.conf man page</a>.
          </p>
        </li>
<li class="list itemizedlist">
          <p class="para">Siehe auch die Seite <a href="https://help.ubuntu.com/community/Kerberos#kerberos-ldap" class="ulink" title="https://help.ubuntu.com/community/Kerberos#kerberos-ldap">Kerberos and LDAP</a> im Ubuntu-Wiki.</p>
        </li>
</ul></div></div></div>
</div></div>
</div>
<div class="links nextlinks">
<a class="nextlinks-prev" href="kerberos.html" title="Kerberos">Zurück</a><a class="nextlinks-next" href="sssd-ad.html" title="SSSD and Active Directory">Weiter</a>
</div>
<div class="clear"></div>
</div>
<div id="pagebottom"></div>
</div></div>
</div>
<div id="footer"><p>The material in this document is available under a free license, see <a href="../../legal.html">Legal</a> for details.<br>
          For information on contributing see the <a href="https://wiki.ubuntu.com/DocumentationTeam">Ubuntu Documentation Team wiki page</a>.
          To report errors in this serverguide documentation, <a href="https://bugs.launchpad.net/serverguide">file a bug report</a>.</p></div>
</div>
</body>
</html>
