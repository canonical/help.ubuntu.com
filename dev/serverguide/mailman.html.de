<!DOCTYPE html>
<html lang=de>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<title>Mailman</title>
<link rel="stylesheet" type="text/css" href="de.css">
<script type="text/javascript" src="jquery.js"></script><script type="text/javascript" src="jquery.syntax.js"></script><script type="text/javascript" src="yelp.js"></script>
</head>
<body id="home">
<script src="https://ssl.google-analytics.com/urchin.js" type="text/javascript"></script><script type="text/javascript">
        _uacct = "UA-1018242-8";
        urchinTracker();
      </script><script>
      function englishPageVersion() {
        var href = window.location.href;
        if (href.slice(-1) == "/") {
                window.location = "index.html.en";
        } else {
                window.location = href.replace(/\.html.*/, ".html.en");
        }
         return false;
      }
      function browserPreferredLanguage() {
        var href = window.location.href;
        if (href.slice(-1) == "/") {
                window.location = href;
        } else {
                window.location = href.replace(/\.html.*/, ".html");
        }
        return false;
      }
      </script><div id="container">
<div id="container-inner">
<div id="mothership"><ul>
<li><a href="https://partners.ubuntu.com">Partners</a></li>
<li><a href="https://www.ubuntu.com/support/community-support">Support</a></li>
<li><a href="https://community.ubuntu.com">Community</a></li>
<li><a href="https://www.ubuntu.com">Ubuntu.com</a></li>
</ul></div>
<div id="header">
<h1 id="ubuntu-header"><a href="https://help.ubuntu.com/">Ubuntu Documentation</a></h1>
<ul id="main-menu">
<li><a class="main-menu-item current" href="../../">Official Documentation</a></li>
<li><a href="https://help.ubuntu.com/community/CommunityHelpWiki">Community Help Wiki</a></li>
<li><a href="https://community.ubuntu.com/t/contribute/26">Contribute</a></li>
</ul>
</div>
<div id="menu-search"><div id="search-box">
<noscript><form action="https://www.google.com/cse" id="cse-search-box"><div>
<input type="hidden" name="cx" value="003883529982892832976:e2vwumte3fq"><input type="hidden" name="ie" value="UTF-8"><input type="text" name="q" size="21"><input type="submit" name="sa" value="Search">
</div></form></noscript>
<script>
                document.write('<form action="../../search.html" id="cse-search-box">');
                document.write('  <div>');
                document.write('    <input type="hidden" name="cof" value="FORID:9">');
                document.write('    <input type="hidden" name="cx" value="003883529982892832976:e2vwumte3fq">');
                document.write('    <input type="hidden" name="ie" value="UTF-8">');
                document.write('    <input type="text" name="q" size="21">');
                document.write('    <input type="submit" name="sa" value="Search">');
                document.write('  </div>');
                document.write('</form>');
              </script>
</div></div>
<div class="trails"><div class="trail">
<a href="../../18.04" class="trail">Ubuntu 18.04</a> » <a class="trail" href="index.html.de" title="Ubuntu–Server-Leitfaden">Ubuntu–Server-Leitfaden</a> » <a class="trail" href="email-services.html.de" title="E-Mail-Dienste">E-Mail-Dienste</a> » </div></div>
<div id="cwt-content" class="clearfix content-area"><div id="page">
<div id="content">
<div class="links nextlinks">
<a class="nextlinks-prev" href="dovecot-server.html.de" title="Dovecot Server">Zurück</a><a class="nextlinks-next" href="mail-filtering.html.de" title="E-Mail-Filterung">Weiter</a>
</div>
<div class="hgroup"><h1 class="title">Mailman</h1></div>
<div class="region">
<div class="contents"><p class="para">Mailman ist ein quelloffenes Programm zur Verwaltung von E-Mail-Diskussionslisten und E-Newsletterlisten. Viele quelloffene Mailinglisten (auch die <a href="http://lists.ubuntu.com" class="ulink" title="http://lists.ubuntu.com">Ubuntu-Mailinglisten</a>) benutzen Mailman als Software für Mailinglisten. Es ist leistungsstark sowie einfach zu installieren und zu warten.</p></div>
<div class="links sectionlinks" role="navigation"><ul>
<li class="links"><a class="xref" href="mailman.html.de#mailman-installation" title="Installation">Installation</a></li>
<li class="links"><a class="xref" href="mailman.html.de#mailman-configuration" title="Konfiguration">Konfiguration</a></li>
<li class="links"><a class="xref" href="mailman.html.de#mailman-admin" title="Verwaltung">Verwaltung</a></li>
<li class="links"><a class="xref" href="mailman.html.de#mailman-users" title="Benutzer">Benutzer</a></li>
<li class="links"><a class="xref" href="mailman.html.de#mailman-references" title="Referenzen">Referenzen</a></li>
</ul></div>
<div class="sect2 sect" id="mailman-installation"><div class="inner">
<div class="hgroup"><h2 class="title">Installation</h2></div>
<div class="region">
<div class="contents">
<p class="para">Mailman stellt eine Web-Schnittstelle für Administratoren und Benutzer zur Verfügung, indem es einen externen Mailserver zum Versenden und Empfangen von E-Mails nutzt. Es läuft einwandfrei mithilfe der folgenden Mailserver:</p>
<p class="para">
            <div class="list itemizedlist"><ul class="list itemizedlist compact">
<li class="list itemizedlist">
                    <p class="para"><span class="app application">Postfix</span></p>
                </li>
<li class="list itemizedlist">
                    <p class="para"><span class="app application">Exim</span></p>
                </li>
<li class="list itemizedlist">
                    <p class="para"><span class="app application">Sendmail</span></p>
                </li>
<li class="list itemizedlist">
                    <p class="para"><span class="app application">Qmail</span></p>
                </li>
</ul></div>
            </p>
<p class="para">In dieser Einführung wird die Installation und Einrichtung von Mailman mit dem Apache-Webserver und dem Postfix- oder Exim-Mailserver beschrieben. Wenn Sie Mailman mit einem anderen Mailserver nutzen möchten, nutzen Sie den Abschnitt "Verweise".</p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
	       <p class="para">Sie müssen nur einen Mailserver installieren. <span class="app application">Postfix</span> ist in Ubuntu der Standard-Mail-Transfer-Agent.</p>
	     </div></div></div></div>
</div>
<div class="sect3 sect" id="mailman-apache2"><div class="inner">
<div class="hgroup"><h3 class="title">Apache2</h3></div>
<div class="region"><div class="contents"><p class="para">
                To install apache2 you refer to <a class="xref" href="httpd.html.de#http-installation" title="Installation">Installation</a> for details.
              </p></div></div>
</div></div>
<div class="sect3 sect" id="mailman-postfix"><div class="inner">
<div class="hgroup"><h3 class="title">Postfix</h3></div>
<div class="region"><div class="contents"><p class="para">Die Anweisungen zur Installation und Konfiguration von Postfix finden Sie unter <a class="xref" href="postfix.html.de" title="Postfix">Postfix</a>.</p></div></div>
</div></div>
<div class="sect3 sect" id="mailman-exim4"><div class="inner">
<div class="hgroup"><h3 class="title">Exim4</h3></div>
<div class="region"><div class="contents">
<p class="para">Die Anleitung für die Installation von Exim4 finden Sie unter <a class="xref" href="exim4.html.de" title="Exim4">Exim4</a>.</p>
<p class="para">
	      Once exim4 is installed, the configuration files are stored in
	      the <span class="file filename">/etc/exim4</span> directory. In Ubuntu, by default, the exim4 configuration files are
	      split across different files. You can change this behavior by changing
	      the following variable in the <span class="file filename">/etc/exim4/update-exim4.conf</span> file:
          </p>
<div class="code"><pre class="contents ">dc_use_split_config='true'
</pre></div>
</div></div>
</div></div>
<div class="sect3 sect" id="mailman-mailman"><div class="inner">
<div class="hgroup"><h3 class="title">Mailman</h3></div>
<div class="region"><div class="contents">
<p class="para">Um <span class="app application">Mailman</span> zu installieren, führen Sie den folgenden Befehl in einem Terminal aus:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo apt install mailman</span> 
</pre></div>
<p class="para">Die Installationsdateien werden in den Ordner <span class="app application">/var/lib/mailman</span> kopiert. Die CGI-Skripte werden im Ordner <span class="app application">/usr/lib/cgi-bin/mailman</span> installiert. Außerdem wird der Linux-Benutzer <span class="em emphasis">list</span> und die Gruppe <span class="em emphasis">list</span> erzeugt. Der Mailman-Prozess wird unter diesem Benutzer ausgeführt.</p>
</div></div>
</div></div>
</div>
</div></div>
<div class="sect2 sect" id="mailman-configuration"><div class="inner">
<div class="hgroup"><h2 class="title">Konfiguration</h2></div>
<div class="region">
<div class="contents"><p class="para">Dieser Abschnitt setzt voraus, dass <span class="app application">Mailman</span>, <span class="app application">Apache2</span> und <span class="app application">Postfix</span> oder <span class="app application">Exim4</span> erfolgreich installiert wurden. Nun müssen diese eingerichtet werden.</p></div>
<div class="sect3 sect" id="mailman-conf-apache2"><div class="inner">
<div class="hgroup"><h3 class="title">Apache2</h3></div>
<div class="region"><div class="contents">
<p class="para"><span class="app application">Mailman</span> stellt eine Beispielsdatei für die Einrichtung von Apache bereit. Sie liegt unter <span class="file filename">/etc/mailman/apache.conf</span>. Damit Apache diese Datei nutzen kann, muss sie in den Ordner <span class="file filename">/etc/apache2/sites-available</span> kopiert werden:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo cp /etc/mailman/apache.conf /etc/apache2/sites-available/mailman.conf</span>
</pre></div>
<p class="para">Dadurch wird ein neuer <span class="em emphasis">VirtualHost</span> in Apache für die Mailman-Einrichtung eingerichtet. Aktivieren Sie nun die Einrichtung und starten Sie Apache neu:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo a2ensite mailman.conf</span>
<span class="cmd command">sudo systemctl restart apache2.service</span>
</pre></div>
<p class="para">Mailman nutzt Apache2, um die CGI-Skripte auszuführen. Die Mailman-CGI-Skripte befinden sich im Ordner <span class="app application">/usr/lib/cgi-bin/mailman</span>. Die Mailman-URL lautet daher http://hostname/cgi-bin/mailman/. Sie können dies in der Datei <span class="file filename">/etc/apache2/sites-available/mailman.conf</span> ändern.</p>
</div></div>
</div></div>
<div class="sect3 sect" id="mailman-conf-postfix"><div class="inner">
<div class="hgroup"><h3 class="title">Postfix</h3></div>
<div class="region"><div class="contents">
<p class="para">Um <span class="app application">Postfix</span> zu nutzen, wird die Domäne lists.example.com für die Mailinglisten eingerichtet. Ersetzen Sie <span class="em emphasis">lists.example.com</span> mit Ihrer gewünschten Domäne.</p>
<p class="para">Sie können den Befehl «postconf» verwenden, um die notwendige Konfiguration zu <span class="file filename">/etc/postfix/main.cf</span> hinzuzufügen:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo postconf -e 'relay_domains = lists.example.com'</span>
<span class="cmd command">sudo postconf -e 'transport_maps = hash:/etc/postfix/transport'</span>
<span class="cmd command">sudo postconf -e 'mailman_destination_recipient_limit = 1'</span>
</pre></div>
<p class="para">Stellen Sie sicher, dass in der Datei <span class="file filename">/etc/postfix/master.cf</span> die folgenen Einträge enthalten sind:</p>
<div class="code"><pre class="contents ">mailman   unix  -       n       n       -       -       pipe
  flags=FR user=list argv=/usr/lib/mailman/bin/postfix-to-mailman.py
  ${nexthop} ${user}
</pre></div>
<p class="para">Dadurch wird das Skript <span class="em emphasis">postfix-to-mailman.py</span> aufgerufen, wenn eine E-Mail an eine Mailingliste gesendet wurde.</p>
<p class="para">Weisen Sie Mailman die Domäne lists.example.com mithilfe der Transportmap zu. Ändern Sie dazu die Datei <span class="file filename">/etc/postfix/transport</span>:</p>
<div class="code"><pre class="contents ">lists.example.com      mailman:
</pre></div>
<p class="para">Mit dem folgenden Befehl auf der Befehlszeile wird nun <span class="app application">Postfix</span> angewiesen, die Transportmap neu zu erstellen:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo postmap -v /etc/postfix/transport</span>
</pre></div>
<p class="para">Starten Sie Postfix neu, um die neue Einrichtung zu aktivieren:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo systemctl restart postfix.service</span>
</pre></div>
</div></div>
</div></div>
<div class="sect3 sect" id="mailman-conf-exim4"><div class="inner">
<div class="hgroup"><h3 class="title">Exim4</h3></div>
<div class="region"><div class="contents">
<p class="para">Nachdem Exim4 installiert wurde, können Sie den Server starten, indem Sie den folgenden Befehl auf der Befehlszeile ausführen:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo systemctl start exim4.service</span>
</pre></div>
<p class="para">Damit Mailman mit Exim4 zusammenarbeitet, müssen Sie Exim4 entsprechend einrichten. Wie bereits zuvor erwähnt, nutzt Exim4 standardmäßig mehrere Einrichtungsdateien für verschiedene Einrichtungsarten: Für weitere Details sei auf die Webseite <a href="http://www.exim.org" class="ulink" title="http://www.exim.org">Exim</a> verwiesen. Um Mailman auszuführen sollte eine neue Einrichtungsdatei den folgenden Einrichtungstypen hinzugefügt werden: <div class="list itemizedlist"><ul class="list itemizedlist compact">
<li class="list itemizedlist">
                <p class="para">Allgemein</p>
              </li>
<li class="list itemizedlist">
                <p class="para">Transport</p>
              </li>
<li class="list itemizedlist">
                <p class="para">Router</p>
              </li>
</ul></div> Exim erstellt eine Haupteinrichtungsdatei mit der Sortierungsreihenfolge der verschiedenen kleinen Einrichtungsdateien. Daher ist die Reihenfolge der Dateien sehr wichtig.</p>
</div></div>
</div></div>
<div class="sect3 sect" id="mailman-conf-main"><div class="inner">
<div class="hgroup"><h3 class="title">Allgemein</h3></div>
<div class="region"><div class="contents"><p class="para">Alle Einrichtungsdateien, die zum Haupttyp gehören, sind im Ordner <span class="file filename">/etc/exim4/conf.d/main/</span> gespeichert. Sie können die folgenden Anweisungen in eine neue Datei mit dem Namen <span class="file filename">04_exim4-config_mailman</span> einfügen: <div class="code"><pre class="contents "># start
# Home dir for your Mailman installation -- aka Mailman's prefix
# directory.
# On Ubuntu this should be "/var/lib/mailman"
# This is normally the same as ~mailman
MM_HOME=/var/lib/mailman
#
# User and group for Mailman, should match your --with-mail-gid
# switch to Mailman's configure script.  Value is normally "mailman"
MM_UID=list
MM_GID=list
#
# Domains that your lists are in - colon separated list
# you may wish to add these into local_domains as well
domainlist mm_domains=hostname.com
#
# -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=
#
# These values are derived from the ones above and should not need
# editing unless you have munged your mailman installation
#
# The path of the Mailman mail wrapper script
MM_WRAP=MM_HOME/mail/mailman
#
# The path of the list config file (used as a required file when
# verifying list addresses)
MM_LISTCHK=MM_HOME/lists/${lc::$local_part}/config.pck
# end
</pre></div></p></div></div>
</div></div>
<div class="sect3 sect" id="mailman-conf-transport"><div class="inner">
<div class="hgroup"><h3 class="title">Transport</h3></div>
<div class="region"><div class="contents"><p class="para">Alle Einrichtungsdateien die zum Transporttyp gehören, sind im Ordner <span class="file filename">/etc/exim4/conf.d/transport/</span> gespeichert. Sie können die folgenden Anweisungen in eine neue Datei mit dem Namen <span class="file filename">40_exim4-config_mailman</span> einfügen: <div class="code"><pre class="contents ">  mailman_transport:
   driver = pipe
   command = MM_WRAP \
               '${if def:local_part_suffix \
                     {${sg{$local_part_suffix}{-(\\w+)(\\+.*)?}{\$1}}} \
                     {post}}' \
               $local_part
    current_directory = MM_HOME
    home_directory = MM_HOME
    user = MM_UID
    group = MM_GID
</pre></div></p></div></div>
</div></div>
<div class="sect3 sect" id="mailman-conf-router"><div class="inner">
<div class="hgroup"><h3 class="title">Router</h3></div>
<div class="region"><div class="contents">
<p class="para">Alle Einrichtungsdateien, die zum Vermittlungstyp gehören, sind im Ordner <span class="file filename">/etc/exim4/conf.d/router/</span> gespeichert. Sie können die folgenden Anweisungen in eine neue Datei mit den Namen <span class="file filename">101_exim4-config_mailman</span> einfügen: <div class="code"><pre class="contents ">  mailman_router:
   driver = accept
   require_files = MM_HOME/lists/$local_part/config.pck
   local_part_suffix_optional
   local_part_suffix = -bounces : -bounces+* : \
                       -confirm+* : -join : -leave : \
                       -owner : -request : -admin
   transport = mailman_transport
</pre></div></p>
<div class="note note-warning" title="Warnung"><div class="inner"><div class="region"><div class="contents">
<p class="para">Die Reihenfolge der Einrichtungsdateien vom Haupt- und Transporttyp kann beliebig sein. Aber die Reihenfolge bei den Routereinrichtungsdateien muss die gleiche sein. Diese bestimmte Datei muss vor der Datei <span class="app application">200_exim4-config_primary</span> geführt sein. Die beiden Einrichtungsdateien enthalten dieselbe Art von Informationen. Die erste Datei hat Vorrang. Weitere Details finden Sie unter dem Abschnitt &gt;&gt;Verweise.</p>
</div></div></div></div>
</div></div>
</div></div>
<div class="sect3 sect" id="mailman-conf-mailman"><div class="inner">
<div class="hgroup"><h3 class="title">Mailman</h3></div>
<div class="region"><div class="contents">
<p class="para">Sobald Sie mailman installiert haben, können Sie es mit folgendem Befehl verwenden:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo systemctl start mailman.service</span>
</pre></div>
<p class="para">Sobald Sie mailman installiert haben, sollten Sie die Standardmailingliste erstellen. Führen Sie dafür folgenden Befehl aus:</p>
<div class="screen"><pre class="contents "><span class="cmd command">sudo /usr/sbin/newlist mailman</span>
</pre></div>
<div class="code"><pre class="contents ">  Enter the email address of the person running the list: bhuvan at ubuntu.com
  Initial mailman password:
  To finish creating your mailing list, you must edit your <span class="file filename">/etc/aliases</span> (or
  equivalent) file by adding the following lines, and possibly running the
  `newaliases' program:

  ## mailman mailing list
  mailman:              "|/var/lib/mailman/mail/mailman post mailman"
  mailman-admin:        "|/var/lib/mailman/mail/mailman admin mailman"
  mailman-bounces:      "|/var/lib/mailman/mail/mailman bounces mailman"
  mailman-confirm:      "|/var/lib/mailman/mail/mailman confirm mailman"
  mailman-join:         "|/var/lib/mailman/mail/mailman join mailman"
  mailman-leave:        "|/var/lib/mailman/mail/mailman leave mailman"
  mailman-owner:        "|/var/lib/mailman/mail/mailman owner mailman"
  mailman-request:      "|/var/lib/mailman/mail/mailman request mailman"
  mailman-subscribe:    "|/var/lib/mailman/mail/mailman subscribe mailman"
  mailman-unsubscribe:  "|/var/lib/mailman/mail/mailman unsubscribe mailman"

  Hit enter to notify mailman owner...

  # 
</pre></div>
<p class="para">Wir haben entweder Postfix oder Exim4 so konfiguriert, dass alle E-Mails von Mailman erkannt werden. Daher ist es nicht notwendig, neue Einträge in <span class="file filename">/etc/aliases</span> zu machen. Wenn Sie irgendwelche Änderungen an den Konfigurationsdateien durchführen, dann sollten Sie sicherstellen, diese Prozesse neu zu starten, bevor Sie mit dem nächsten Abschnitt fortfahren.</p>
<div class="note" title="Anmerkung"><div class="inner"><div class="region"><div class="contents">
         <p class="para">Exim4 nutzt keiner der obigen Pseudonymen, um E-Mails an Mailman weiterzuleiten, da es den <span class="em emphasis">»discover«</span>-Ansatz benutzt. Um die Pseudonymen zu unterdrücken, während die Liste erstellt wird, können Sie die Zeile <span class="em emphasis">MTA=None</span> in der Mailman-Konfigurationsdatei <span class="file filename">/etc/mailman/mm_cfg.py</span> hinzufügen.</p>
         </div></div></div></div>
</div></div>
</div></div>
</div>
</div></div>
<div class="sect2 sect" id="mailman-admin"><div class="inner">
<div class="hgroup"><h2 class="title">Verwaltung</h2></div>
<div class="region"><div class="contents">
<p class="para">Wir nehmen an, dass Sie eine Standardinstallation haben. Die Mailman-CGI-Skripte befinden sich immer noch im Verzeichnis <span class="app application">/usr/lib/cgi-bin/mailman/</span>. Mailman liefert eine webbasierte Konfigurationsoberfläche. Um auf diese Seite zuzugreifen, greifen Sie mit Ihrem Browser auf folgende URL zu:</p>
<p class="para">http://hostname/cgi-bin/mailman/admin</p>
<p class="para">Die vorgegebene Mailingliste, <span class="em emphasis">mailman</span> wird auf dem Bildschirm erscheinen. Wenn Sie auf den Listennamen zugreifen, werden Sie nach einem Kennwort zur Authentifizierung gefragt. Wenn Sie das korrekte Passwort eingeben, können Sie die Systemverwaltungseinstellungen in dieser Liste ändern. Sie können eine neue Mailingliste mit dem Kommandozeilenprogramm (<span class="cmd command">/usr/sbin/newlist</span>) erstellen. Alternativ können Sie eine neue Mailingliste über die Weboberfläche erzeugen.</p>
</div></div>
</div></div>
<div class="sect2 sect" id="mailman-users"><div class="inner">
<div class="hgroup"><h2 class="title">Benutzer</h2></div>
<div class="region"><div class="contents">
<p class="para">Mailman liefert eine webbasierte Oberfläche für die Benutzer. Um auf diese Seite zuzugreifen, geben Sie die folgende URL in Ihrem Browswer ein:</p>
<p class="para">http://hostname/cgi-bin/mailman/listinfo</p>
<p class="para">Die vorgegebene Mailingliste, <span class="em emphasis">mailman</span>, wird auf dem Schirm erscheinen. Wenn Sie auf den Listennamen klicken, wird das Anmeldeformular angezeigt. Sie können Ihre E-Mail-Adresse, Ihren Namen (optional) und Ihr Kennwort eingeben, um diese zu abonnieren. Eine Einladung wird per E-Mail an Sie gesandt. Sie können die Anweisungen in der E-Mail befolgen, um sich anzumelden.</p>
</div></div>
</div></div>
<div class="sect2 sect" id="mailman-references"><div class="inner">
<div class="hgroup"><h2 class="title">Referenzen</h2></div>
<div class="region"><div class="contents">
<p class="para">
<a href="http://www.list.org/mailman-install/index.html" class="ulink" title="http://www.list.org/mailman-install/index.html">GNU Mailman-Installationshandbuch</a>
</p>
<p class="para">
<a href="http://www.exim.org/howto/mailman21.html" class="ulink" title="http://www.exim.org/howto/mailman21.html">HOWTO - Verwendung von Exim4 und Mailman 2.1 zusammen.</a>
</p>
<p class="para">Siehe auch die <a href="https://help.ubuntu.com/community/Mailman" class="ulink" title="https://help.ubuntu.com/community/Mailman">Mailman-Seite</a> im Ubuntu-Wiki.</p>
</div></div>
</div></div>
</div>
<div class="links nextlinks">
<a class="nextlinks-prev" href="dovecot-server.html.de" title="Dovecot Server">Zurück</a><a class="nextlinks-next" href="mail-filtering.html.de" title="E-Mail-Filterung">Weiter</a>
</div>
<div class="clear"></div>
</div>
<div id="pagebottom"></div>
</div></div>
</div>
<div id="footer">
<p style="padding-bottom: 0.4em">You can choose the <b>displayed language</b> by adding a language suffix to the web address
          so it ends with e.g. <tt>.html.en</tt> or <tt>.html.de</tt>.<br>
          If the web address has no language suffix, the preferred language specified in your web browser's settings is used. For your convenience:<br>
          [ <a title="English page version" href="#" onClick="englishPageVersion();">Change to English Language</a> | 
          <a title="Language selected by browser" href="#" onClick="browserPreferredLanguage()">Change to Browser's Preferred Language</a> ]</p>
<p>The material in this document is available under a free license, see <a href="../../legal.html">Legal</a> for details.<br>
          For information on contributing see the <a href="https://wiki.ubuntu.com/DocumentationTeam">Ubuntu Documentation Team wiki page</a>.
          To report errors in this serverguide documentation, <a href="https://bugs.launchpad.net/serverguide">file a bug report</a>.</p>
</div>
</div>
</body>
</html>
